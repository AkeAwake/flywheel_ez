#include "main.h"

/*pros::Rotation r_sensor(15);

// This is the height to push into goal, find in catapult.cpp
int degree1M = 55;

// This is a little bit lower than the height the catapult is at the highest at, find in catapult.cpp
int topheightM = 65;

void setMatchload(){

    if(controller.get_digital(pros::E_CONTROLLER_DIGITAL_L1) and controller.get_digital(pros::E_CONTROLLER_DIGITAL_R2) and (Catapult = 0)){

        Catapult = 127;

    }

    if(controller.get_digital(pros::E_CONTROLLER_DIGITAL_L1) and controller.get_digital(pros::E_CONTROLLER_DIGITAL_R2) and (Catapult = 127)){
    
        if((r_sensor.get_angle()/100) < degree1M){
            
            Catapult = 0;

        }

        if((r_sensor.get_angle()/100) < topheightM and (r_sensor.get_angle()/100) > degree1M){

            Catapult = 60;

        }

    }

}*/